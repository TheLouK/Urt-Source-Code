Index: code/server/sv_client.c
===================================================================
--- code/server/sv_client.c	(revision 830)
+++ code/server/sv_client.c	(working copy)
@@ -1649,6 +1649,14 @@
 		if (!u->name && sv.state == SS_GAME) {
 			Cmd_Args_Sanitize();
 
+			if (cl->muted && (!Q_stricmp("say", Cmd_Argv(0)) ||
+						!Q_stricmp("say_team", Cmd_Argv(0)) ||
+						!Q_stricmp("tell", Cmd_Argv(0)) ||
+						!Q_stricmp("ut_radio", Cmd_Argv(0)) ||
+						!Q_stricmp("callvote", Cmd_Argv(0)))) {
+				SV_SendServerCommand(cl, "print \"You are currently muted and may not perform that action.\n\"");
+				return;
+			}
 			///////////////////////////////////////////////////////////////
 			// separator for mutefix.patch and callvoteconnectprotect.patch
 			///////////////////////////////////////////////////////////////
Index: code/server/server.h
===================================================================
--- code/server/server.h	(revision 830)
+++ code/server/server.h	(working copy)
@@ -199,6 +199,7 @@
 	int				oldServerTime;
 	qboolean			csUpdated[MAX_CONFIGSTRINGS+1];	
 
+	qboolean		muted;
 	///////////////////////////////////////////////
 	// separator for mutefix.patch and ip2loc.patch
 	///////////////////////////////////////////////
Index: code/server/sv_init.c
===================================================================
--- code/server/sv_init.c	(revision 830)
+++ code/server/sv_init.c	(working copy)
@@ -541,6 +541,7 @@
 				// was connected before the level change
 				SV_DropClient( &svs.clients[i], denied );
 			} else {
+				svs.clients[i].muted = qfalse;
 				/////////////////////////////////////////////
 				// separator for mutefix.patch and goto.patch
 				/////////////////////////////////////////////
Index: code/server/sv_main.c
===================================================================
--- code/server/sv_main.c	(revision 830)
+++ code/server/sv_main.c	(working copy)
@@ -198,6 +198,14 @@
 		return;
 	}
 
+	if (cl != NULL) {
+		if (!strcmp((char *) message, "print \"The admin muted you: you cannot talk.\"\n")) {
+			cl->muted = qtrue;
+		}
+		else if (!strcmp((char *) message, "print \"The admin unmuted you.\"\n")) {
+			cl->muted = qfalse;
+		}
+	}
 	//////////////////////////////////////////////////////
 	// separator for mutefix.patch and cyclemaplimit.patch
 	//////////////////////////////////////////////////////
